
describe('App', () => {

    test('should be 30', () => {
        // Arrange
        const a = 10;
        const b = 20;

        // Act
        const result = a + b;

        // Assert
        expect(result).toBe(30);

    })

});
